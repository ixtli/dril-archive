{
  "name": "dril-archive",
  "private": true,
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "dev:full": "bun scripts/dev-full.ts",
    "build": "cd site && bunx vite build",
    "build:testdb": "cargo run -p dril-builder -- testdata/sample.ndjson site/vendor/dril.db",
    "test:e2e": "bunx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "^3.51.2-build8"
  }
}
